{{!--  --}}
<div id="log-img" style="margin-top: 6.55rem; padding-top: 3rem;">
    
    <div id="sign-page">
        {{#if massage}}
            <div class="alert alert-danger" role="alert">{{massage}}</div>
        {{/if}}
        <form method="POST" action="/user/register">
        <div class="form-group">
            <label for="Email1">Địa chỉ Email</label>
            <input style="font-size: 1.5rem; text-transform: none !important;" type="email" name="email" value="{{resinfo.email}}" class="form-control" id="Email1" aria-describedby="emailHelp" placeholder="Enter email"
            pattern="^([0-9a-zA-Z]([-.\w]*[0-9a-zA-Z])*@([0-9a-zA-Z][-\w]*[0-9a-zA-Z]\.)+[a-zA-Z]{2,9})$" required>
        </div>

        <div class="form-group">
            <label for="Password">Mật khẩu</label>
            <input style="font-size: 1.5rem;"  type="password" name="password" class="form-control" id="Password" placeholder="Password" required>
        </div>

        <div class="form-group">
            <label for="confirm-password">Xác nhận Mật khẩu</label>
            <input style="font-size: 1.5rem;" type="password" name="cfpassword" class="form-control" id="confirm-password" placeholder="Password" required>
        </div>
        <div class="form-group">
            <label for="name">Họ và Tên </label>
            <input style="font-size: 1.5rem;" type="text" name="name" value="{{resinfo.name}}" class="form-control" placeholder="Nhập tên" required>
        </div>
        <div class="form-group">
            <label for="gender-input">Giới tính</label>
            <select  name="gender" class="form-control" id="gender-input" required>
                <option  value="">--chọn giới tính--</option>
                <option style="font-size: 1.5rem;" value="male">Nam</option>
                <option style="font-size: 1.5rem;" value="female">Nữ</option>
            </select>
        </div>
        <div class="form-group">
            <label for="phonenumber">Số điện thoại</label>
            <input style="font-size: 1.5rem;" type="tel" name="phonenumber" class="form-control" placeholder="Nhập số điện thoại" value="{{resinfo.phonenumber}}"
            pattern="^(0[234][0-9]{8}|1[89]00[0-9]{4})$" required>
        </div>
        <button style="font-size: 1.5rem; width: 100%;" type="submit" class="btn btn-primary " style="text-align: center;width: 100%;">Submit</button>
    </form>
    </div>
</div>
<style>
#sign-page{
    padding: 16px 24px; 
    width: 75rem; 
    margin: auto; 
    border: 4px solid rgba(123, 213, 231, 0.3); 
    border-radius: 8px;
    font-size: 1.5rem;
}
#log-img{
   background: url(/image/home-bg.jpg) top center / cover no-repeat !important;
    height: 54rem;
}

</style>